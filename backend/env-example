# Uygulama Portu
PORT=5070

# PostgreSQL Veritabanı Ayarları
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123456
DB_NAME=biglicence

# JWT Secret Key (Güçlü bir key kullanın!)
JWT_SECRET=super-secret-key-change-this-in-production

# SMTP / Mail Ayarları
# Kendi SMTP sunucunuza göre bu değerleri güncelleyin
SMTP_HOST=smtp.ornek.com
SMTP_PORT=587
SMTP_USER=no-reply@ornek.com
SMTP_PASS=parolanizi_buraya_yazin
MAIL_FROM="Lisans Portalı <no-reply@ornek.com>"
# Grup Mail Adresi (Opsiyonel - Mail gönderildiğinde bu adrese de BCC olarak mail atılır)
MAIL_GROUP=group@ornek.com